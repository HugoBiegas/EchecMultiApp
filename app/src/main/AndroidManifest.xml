<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.echec.echecmulti">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EchecMulti">
        <activity
            android:name=".Connection.connexiontest"
            android:exported="true" />
        <activity
            android:name=".Connection.Profile"
            android:exported="true" />
        <activity
            android:name=".Connection.Register"
            android:exported="true" />
        <activity
            android:name=".Room.CreaRoom"
            android:exported="true" />
        <activity
            android:name=".GameActivity"
            android:exported="false" />
        <activity
            android:name=".Room.RoomActivity"
            android:exported="true" />
        <activity
            android:name=".Connection.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>